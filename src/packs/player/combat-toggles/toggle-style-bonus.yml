_id: toggleStyleBonus
_key: '!macros!toggleStyleBonus'
_stats:
  coreVersion: '13'
  lastModifiedBy: teriockBuilder00
  systemId: teriock
  systemVersion: 0.9.16
author: teriockBuilder00
command: |-
  if (actor) {
    await actor.update({
      "system.offense.sb": !actor.system.offense.sb,
    });
    if (actor.system.offense.sb) {
      ui.notifications.success(`Style bonus enabled for ${actor.name}.`);
    } else {
      ui.notifications.info(`Style bonus disabled for ${actor.name}.`);
    }
  } else {
    ui.notifications.warn("No actor selected.");
  }
folder: combatToggles000
img: systems/teriock/src/icons/properties/weapon.webp
name: Toggle Style Bonus
scope: global
type: script
