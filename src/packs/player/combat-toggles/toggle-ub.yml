name: Toggle UB
type: script
_id: WHcf5MyaFkJDeb4f
author: teriockBuilder00
img: systems/teriock/src/icons/properties/ub.webp
scope: global
command: |-
  if (actor) {
    if (actor.system.offense.piercing === "ub") {
      await actor.update({
        "system.offense.piercing": "none",
      });
    } else {
      await actor.update({
        "system.offense.piercing": "ub",
      });
    }
    if (actor.system.offense.piercing === "ub") {
      ui.notifications.success(`UB piercing enabled for ${actor.name}.`);
    } else {
      ui.notifications.info(`UB piercing disabled for ${actor.name}.`);
    }
  } else {
    ui.notifications.warn("No actor selected.");
  }
ownership:
  default: 0
flags: {}
_stats:
  coreVersion: '13'
  systemId: teriock
  systemVersion: 0.9.0
  lastModifiedBy: teriockBuilder00
  compendiumSource: null
folder: X4L6z0GymT4kGE6f
_key: '!macros!WHcf5MyaFkJDeb4f'
