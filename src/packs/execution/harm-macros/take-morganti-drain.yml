_id: FvsCmP4KXDOhXSrb
_key: '!macros!FvsCmP4KXDOhXSrb'
_stats:
  coreVersion: '14'
  lastModifiedBy: teriockBuilder00
  systemId: teriock
  systemVersion: 0.10.5
command: >-
  const amount = scope.message.system.activations.find((a) => a.type === "take"
  && a.take === "drain")?.amount || 0;

  for (const a of scope.actors || []) {
    if (a) {
      await a.system.takeDrain(amount, { morganti: true });
    }
  }
folder: KDIQAjqFbSAKvi05
img: icons/svg/dice-target.svg
name: Take Morganti Drain
scope: global
type: script
