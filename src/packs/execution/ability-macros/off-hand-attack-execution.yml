_id: 29b994da873c13a9
_key: '!macros!29b994da873c13a9'
_stats:
  coreVersion: '14'
  lastModifiedBy: teriockBuilder00
  systemId: teriock
  systemVersion: 0.12.83
command: >-
  const options = scope.execution.options;

  const actor = scope.execution.actor;

  const armaments = actor.armaments.filter((a) => a.active &&
  actor.system.wielding.attacker?.id !== a.id);

  if (armaments.length > 0) {
    const selectedArmament = await tm.dialogs.selectDocumentDialog(armaments, {
      hint: _loc("TERIOCK.DIALOGS.Select.Armament.hint"),
      title: _loc("TERIOCK.DIALOGS.Select.Armament.title"),
    });
    let abilities = [...actor.abilities, ...game.teriock.basicAbilities];
    abilities = abilities.filter((a) =>
      a.system.interaction === "attack"
      && a.system.maneuver === "active"
      && a.system.executionTime.base === "a1"
      && ["hand", "weapon"].includes(a.system.delivery)
    ).sort((a, b) => a.name.localeCompare(b.name));
    const ability = await tm.dialogs.selectDocumentDialog(abilities, {
      hint: _loc("TERIOCK.DIALOGS.Select.Ability.hint"),
      title: _loc("TERIOCK.DIALOGS.Select.Ability.title"),
    });
    options.armament = selectedArmament;
    await ability.system.use(options);
  }
folder: SsB1D27Gn6q9lTTV
img: icons/svg/dice-target.svg
name: Off-Hand Attack - Execution
scope: global
type: script
