/**
 * Options for pulling data from https://wiki.teriock.com.
 *
 * @property notify - If true, display notifications for the pull process.
 */
export type WikiPullOptions = {
  notify?: boolean;
};
