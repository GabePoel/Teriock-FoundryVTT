import { preLocalize } from "../../helpers/localization.mjs";

export const timeOptions = {
  triggers: {
    dawn: "TERIOCK.TERMS.Triggers.dawn",
    dusk: "TERIOCK.TERMS.Triggers.dusk",
    longRest: "TERIOCK.TERMS.Triggers.longRest",
    shortRest: "TERIOCK.TERMS.Triggers.shortRest",
  },
};

preLocalize("options.time.triggers");
