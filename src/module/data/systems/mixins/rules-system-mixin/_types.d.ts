declare global {
  namespace Teriock.Models {
    export interface RulesSystemData {
      /** A kebab-case string that uniquely identifies this rules element amongst its type. */
      identifier: Identifier;
    }
  }
}

export {};
