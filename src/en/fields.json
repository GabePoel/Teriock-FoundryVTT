{
  "TERIOCK": {
    "FIELDS": {
      "PseudoCollectionField": {
        "notPseudoDocument": "The model must be a PseudoDocument",
        "noTypes": "Either the model must define types or they must be explicitly provided"
      }
    }
  }
}
