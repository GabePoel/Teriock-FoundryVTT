{{#each displayFields}}
  {{formGroup
    schema
    classes=classes
    dataset=dataset
    disabled=(not (and @root/editable editable))
    enriched=enriched
    label=label
    value=value
    rootId=@root/uuid
  }}
{{/each}}