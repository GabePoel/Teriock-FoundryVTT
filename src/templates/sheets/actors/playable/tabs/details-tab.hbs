<section class="actor-tab notes tab {{ tabClass }}" data-group="primary" data-tab="details">
  <h1>{{localize "TERIOCK.SHEETS.Actor.TABS.Details.title"}}</h1>
  <div class="actor-tab-page">
    <div class="tgrid g2" style="gap: 1em">
      <div class="teriock-block-display">
        <div
          class="actor-shortbox actor-movement-box"
          data-tooltip-identifier="core:movement-speed"
          data-tooltip-direction="LEFT"
        >
          <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.movementSpeed.label"}}</div>
          <div class="actor-shortbox-value">
            {{{ system.movementSpeed }}}
            {{localize "TERIOCK.SHEETS.Actor.TABS.Details.movementSpeed.rate"}}
          </div>
        </div>
        <div class="teriock-block-display-divider" style="margin-bottom: 1rem"></div>
        <div class="tgrid g3" style="padding-inline: 0.5em; row-gap: 0.55rem">
          {{#each TERIOCK.config.character.movement as |config key|}}
            <div class="gi2">
              {{{ config.label }}}
            </div>
            <div
              class="
                tcolored
                thover
                affix
                {{#if (and
                @root/highlightModified
                (not (eq
                (lookup @root/system.speedAdjustments key)
                (lookup @root/system._source.speedAdjustments key))))}}
                  modified
                {{/if}}
              "
              data-action="updatePaths"
              data-icon="{{ config.icon }}"
              data-paths="system.speedAdjustments.{{ key }}"
            >
              <div class="unselected" style="justify-content: left">
                {{path @root/TERIOCK.config.character.speed (lookup @root/system.speedAdjustments key) "label"}}
              </div>
              <div class="selected" style="justify-content: left">
                {{path @root/TERIOCK.config.character.speed (lookup @root/system._source.speedAdjustments key) "label"}}
              </div>
            </div>
          {{/each}}
        </div>
      </div>
      <div class="tgrid-item" style="display: flex; flex-direction: column; gap: 1em">
        <div class="teriock-block-display" style="padding: 1em">
          <div class="tgrid g3" style="padding-inline: 0.5em">
            <div class="gi2 tsubtle">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.sense.label"}}</div>
            <div class="tcenter tsubtle">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.sense.range"}}</div>
            {{#each TERIOCK.config.character.sense as |config key|}}
              {{#if (not config.hidden)}}
                <div class="gi2">
                  {{{ config.label }}}
                </div>
                <div
                  class="
                    tcenter
                    thover
                    affix
                    {{#if (and
                    @root/highlightModified
                    (not (eq (path ../system.senses key) (path ../system._source.senses key))))}}
                      modified
                    {{/if}}
                  "
                  data-action="updatePaths"
                  data-icon="{{ config.icon }}"
                  data-paths="system.senses.{{ key }}"
                >
                  <div class="unselected">
                    <span class="tcolored tcenter ttransparent">{{path ../system.senses key}} ft</span>
                  </div>
                  <div class="selected">
                    <span class="tcolored tcenter ttransparent">{{path ../system._source.senses key}} ft</span>
                  </div>
                </div>
              {{/if}}
            {{/each}}
          </div>
        </div>
        <div class="teriock-block-display" style="padding-top: 0.75em">
          <div class="carrying-box-main" data-tooltip-identifier="core:size">
            <div class="actor-shortbox actor-size-box">
              <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.size.label"}}</div>
              <div
                class="
                  actor-size-input
                  actor-smallbox-value
                  thover
                  {{#if (and
                  highlightModified
                  (not (eq system.size.value system._source.size.value)))}}
                    tcolored
                  {{/if}}
                "
                data-action="updatePaths"
                data-icon="{{ TERIOCK.display.icons.species.size }}"
                data-paths="system.size.value"
                data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.size.description" category=system.size.category reach=system.size.reach}}'
                data-tooltip-class=""
                data-tooltip-direction="DOWN"
                style="height: 2rem"
              >
                {{{ system.size.value }}}
              </div>
            </div>
            <div class="actor-shortbox actor-weight-box">
              <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.label"}}</div>
              <div
                class="
                  actor-weight-input
                  actor-smallbox-value
                  thover
                  {{#if (and
                  highlightModified
                  (not (eq system.weight.self system._source.weight.self)))}}
                    tcolored
                  {{/if}}
                "
                data-action="updatePaths"
                data-icon="{{ TERIOCK.display.icons.equipment.weight }}"
                data-paths="system.weight.self"
                data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.includingEquipment" value=system.weight.value}}'
                data-tooltip-class=""
                data-tooltip-direction="DOWN"
                style="height: 2rem{{#unless (or (eq system._source.weight.self 0) system._source.weight.self)}}; color: var(--color-text-subtle){{/unless}}"
              >
                {{{ system.weight.self }}}
                {{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.unit"}}
              </div>
            </div>
            <div class="actor-shortbox actor-carrying-box">
              <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.label"}}</div>
              <div
                class="actor-smallbox-value tcenter"
                data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.includingSelf" value=system.weight.value}}'
                data-tooltip-class=""
                data-tooltip-direction="DOWN"
                style="height: 2rem"
              >
                {{{ system.weight.carried }}}
                {{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.unit"}}
              </div>
            </div>
          </div>
          <div class="teriock-block-display-divider" style="margin-top: 0.125em; margin-bottom: 0.625em"></div>
          <div class="tgrid g3" data-tooltip-identifier="core:carrying-capacity">
            <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.light"}}</div>
            <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.heavy"}}</div>
            <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.max"}}</div>
            <div
              class="
                tcenter
                tround
                {{#if (gte system.weight.carried system.carryingCapacity.light)}}
                  tred
                {{/if}}
              "
              data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.lightCapacity"}}'
              data-tooltip-direction="DOWN"
              data-tooltip-class=""
            >
              {{{ system.carryingCapacity.light }}}
              {{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.unit"}}
            </div>
            <div
              class="
                tcenter
                tround
                {{#if (gte system.weight.carried system.carryingCapacity.heavy)}}
                  tred
                {{/if}}
              "
              data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.heavyCapacity"}}'
              data-tooltip-direction="DOWN"
              data-tooltip-class=""
            >
              {{{ system.carryingCapacity.heavy }}}
              {{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.unit"}}
            </div>
            <div
              class="
                tcenter
                tround
                {{#if (gte system.weight.carried system.carryingCapacity.max)}}
                  tred
                {{/if}}
              "
              data-tooltip='{{localize "TERIOCK.SHEETS.Actor.TABS.Details.carrying.maxCapacity"}}'
              data-tooltip-direction="DOWN"
              data-tooltip-class=""
            >
              {{{ system.carryingCapacity.max }}}
              {{localize "TERIOCK.SHEETS.Actor.TABS.Details.weight.unit"}}
            </div>
          </div>
        </div>
      </div>
      <div class="teriock-block-display" style="padding-bottom: 0.25em; padding-top: 0.75em">
        <div class="tgrid g2">
          <div class="actor-shortbox" data-tooltip-identifier="keyword:curse" data-tooltip-direction="LEFT">
            <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.curses.label"}}</div>
            <div
              class="
                actor-smallbox-value
                tcenter
                affix
                thover
                {{#if (and
                highlightModified
                (not (eq system.curses.max system._source.curses.max)))}}
                  modified
                {{/if}}
              "
              data-action="updatePaths"
              data-icon="{{ TERIOCK.display.icons.power.curse }}"
              data-paths="system.curses.max"
              style="height: 2rem"
            >
              <span class="unselected">
                {{localize
                  "TERIOCK.FORMAT.outOf"
                  numerator=system.curses.value
                  denominator=system.curses.max
                }}
              </span>
              <span class="selected">
                {{localize
                  "TERIOCK.FORMAT.outOf"
                  numerator=system.curses.value
                  denominator=system._source.curses.max
                }}
              </span>
            </div>
          </div>
          <div class="actor-shortbox">
            <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.rotators.label"}}</div>
            <div
              class="
                actor-smallbox-value
                tcenter
                affix
                thover
                {{#if (and
                highlightModified
                (not (eq system.rotators.max system._source.rotators.max)))}}
                  modified
                {{/if}}
              "
              data-action="updatePaths"
              data-icon="{{ TERIOCK.display.icons.maneuver.passive }}"
              data-paths="system.rotators.max"
              style="height: 2rem"
            >
              <span class="unselected">
                {{localize
                  "TERIOCK.FORMAT.outOf"
                  numerator=system.rotators.value
                  denominator=system.rotators.max
                }}
              </span>
              <span class="selected">
                {{localize
                  "TERIOCK.FORMAT.outOf"
                  numerator=system.rotators.value
                  denominator=system._source.rotators.max
                }}
              </span>
            </div>
          </div>
        </div>
      </div>
      <div class="teriock-block-display" style="flex-direction: row; padding-bottom: 0.25em; padding-top: 0.75em">
        <div class="actor-shortbox">
          <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.hiding.label"}}</div>
          <div
            class="actor-detection-input actor-smallbox-value thover"
            data-action="updatePaths"
            data-icon="{{ TERIOCK.display.icons.attribute.snk }}"
            data-paths="system.detection.hiding"
            style="height: 2rem{{#unless (or (eq system._source.detection.hiding 0) system._source.detection.hiding)}}; color: var(--color-text-subtle){{/unless}}"
          >
            {{#if (or (eq system._source.detection.hiding 0) system._source.detection.hiding)}}
              {{ system._source.detection.hiding }}
            {{else}}
              {{ system.attributes.snk.passive }}
            {{/if}}
          </div>
        </div>
        <div class="actor-shortbox">
          <div class="tsubtle tcenter">{{localize "TERIOCK.SHEETS.Actor.TABS.Details.perceiving.label"}}</div>
          <div
            class="actor-detection-input actor-smallbox-value thover"
            data-action="updatePaths"
            data-icon="{{ TERIOCK.display.icons.attribute.per }}"
            data-paths="system.detection.perceiving"
            style="height: 2rem{{#unless (or (eq system._source.detection.perceiving 0) system._source.detection.perceiving)}}; color: var(--color-text-subtle){{/unless}}"
          >
            {{#if (or (eq system._source.detection.perceiving 0) system._source.detection.perceiving)}}
              {{ system._source.detection.perceiving }}
            {{else}}
              {{ system.attributes.per.passive }}
            {{/if}}
          </div>
        </div>
      </div>
    </div>
    <div class="teriock-block-display actor-notes-content">
      <prose-mirror name="system.notes" value="{{ system.notes }}" toggled="true" editable="editable">
        {{#if (exists enrichedNotes)}}
          {{{ enrichedNotes }}}
        {{else}}
          {{localize "TERIOCK.SHEETS.Actor.TABS.Details.notes.placeholder"}}
        {{/if}}
      </prose-mirror>
    </div>
  </div>
</section>
