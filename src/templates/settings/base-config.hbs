{{!-- Blatantly stolen from D&D 5E --}}
<fieldset
  {{disabled disabled}}
>
  <legend>{{ifThen legend (localize legend) (localize "SETUP.Configuration")}}</legend>
  {{> "teriock/shared/field-list"
    fields
  }}
</fieldset>
