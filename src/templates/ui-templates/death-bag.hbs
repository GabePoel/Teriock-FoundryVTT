<div class="teriock death-bag-pull dice-roll">
  {{#each pulledStones as |number color|}}
    {{#if (gt number 0)}}
      <div class="death-bag-stones-container">
        <header class="message-header">
          <span class="flavor-text">{{path @root/TERIOCK.index.deathBag color}} Stones</span>
        </header>
        <div class="death-bag-stones dice-formula">
          {{#repeat number}}
            <div
              class="death-bag-stone"
              data-tooltip="{{path @root/TERIOCK.index.deathBag color}} Stone"
            >
              <img
                src="systems/teriock/src/icons/death-bag-stones/{{color}}.webp"
                alt="{{path @root/TERIOCK.index.deathBag color}}"
              >
            </div>
          {{/repeat}}
        </div>
        <div class="dice-result">
          <h4 class="dice-total message-result">{{number}}</h4>
        </div>
      </div>
    {{/if}}
  {{/each}}
</div>