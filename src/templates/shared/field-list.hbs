{{!-- Blatantly stolen from D&D 5E --}}
{{#each this}}
  {{#if name}}{{!-- TODO: Work around core issue with providing undefined name --}}
    {{formGroup field name=name value=value input=input options=options disabled=disabled localize=localize
                placeholder=placeholder label=label hint=hint classes=classes rootId=@root.partId }}
  {{else}}
    {{formGroup field value=value input=input options=options disabled=disabled localize=localize
                placeholder=placeholder label=label hint=hint classes=classes rootId=@root.partId }}
  {{/if}}
{{/each}}
