import { TeriockItemSheet } from './item-sheet.mjs';

/**
 * Extend the basic ItemSheet with some very simple modifications
 * @extends {ItemSheet}
 */
export class EquipmentItemSheet extends TeriockItemSheet {
    /** @override */
    get template() {
        super.template();
    }
}