{{#if (exists system.equipmentClasses)}}
    <div class="ab-tags-container">
        <div class="ab-tags">
            {{#each system.equipmentClasses as |key index|}}<span class="ab-tag equipment-class-tag" value="{{key}}">{{{key}}}</span>{{/each}}
        </div>
    </div>
{{/if}}